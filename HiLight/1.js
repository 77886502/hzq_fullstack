var minOperations = function(nums1, nums2) {
    let num = 0,  = eval(nums1.join("+"))-eval(nums2.join("+"));
    if(sum1 == sum2)
        return 0;
    else
    {
        if(sum1 > sum2)
        {
            if(nums1.length > nums2.length*6)
                return -1;
            let average1 = Math.ceil(sum1/nums1.length),average2 = Math.floor(sum2/nums2.length);
            if(average1-1>6-average2)
            {
                if((sum1-sum2)/average1)
            }
        }
        else
        {
            if(nums1.length*6 < nums2.length)
                return -1;
            
           
        }
    }
    return num;
};
console.log(minOperations([2,3,6,4,5],[6,7,2,6,9]));